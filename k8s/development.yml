apiVersion: v1
kind: Service
metadata:
    name: game-panstwa-miasta-backend-dev
spec:
    ports:
        - port: 80
          targetPort: 80
    selector:
        app: game-panstwa-miasta-backend-dev
---
apiVersion: apps/v1
kind: Deployment
metadata:
    name: game-panstwa-miasta-backend-dev
spec:
    selector:
        matchLabels:
            app: game-panstwa-miasta-backend-dev
    replicas: 1
    template:
        metadata:
            labels:
                app: game-panstwa-miasta-backend-dev
        spec:
            containers:
                - name: game-panstwa-miasta-backend-dev
                  image: registry.gitlab.com/enlighten1/capgemini-capmania/panstwa-miasta-backend:latest-development
                  imagePullPolicy: Always
                  ports:
                      - containerPort: 80
                  env:
                      - name: TZ
                        value: Europe/Warsaw
                  resources:
                      requests:
                          memory: '40Mi'
                          cpu: '20m'
                      limits:
                          memory: '100Mi'
                          cpu: '100m'
            imagePullSecrets:
                - name: regcred
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
    name: game-panstwa-miasta-backend-dev-ingress
    annotations:
        kubernetes.io/ingress.class: nginx
        cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
    tls:
        - hosts:
              - panstwa-miasta-backend-dev.capgemini.enl-projects.com
          secretName: game-panstwa-miasta-backend-dev-tls
    rules:
        - host: panstwa-miasta-backend-dev.capgemini.enl-projects.com
          http:
              paths:
                  - backend:
                        serviceName: game-panstwa-miasta-backend-dev
                        servicePort: 80
